version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd front-end
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: front-end/dist
    files:
      - '**/*'
  cache:
    paths:
      - front-end/node_modules/**/*
      - front-end/.npm/**/*
